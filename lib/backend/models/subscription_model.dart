// Define the backend model for subscription if required
// Add necessary fields to represent the subscription data
class BackendSubscriptionModel {
  // Define properties for backend subscription model
}

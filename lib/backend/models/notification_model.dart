// Define the structure of a notification item in the backend
class NotificationModel {
  final int id;
  final String title;
  final String content;

  NotificationModel({
    required this.id,
    required this.title,
    required this.content,
  });
}
